<!DOCTYPE html>
<html>
<head>
<!-- <link rel="stylesheet" href="/css/main.css"> -->
</head>
<body style="font-size:15px; margin: 10px;">

  <% include ./partials/_header %>

  <h2>Here is the list of URLs:</h2>
    <div class="container">
    <ol>
      <div>
        <% for (var shortURL in urls) {  %>
          <li>
            <a href="http://localhost:8080/u/<%= shortURL %>">
              <%= shortURL %>
            </a>
            &nbsp; &#8594; &nbsp;
            <%= urls[shortURL].url %>

            <div class="row">
              <div>
                  <form method="GET" action="/urls/<%= shortURL %>/update">
                    <button type="submit" name="updateButton"> Update </button>
                  </form>
              </div>
              <div>
                  <form method="POST" action="/urls/<%= shortURL %>/delete">
                    <button type="submit" name="deleteButton"> Delete </button>
                  </form>
              </div>
            </div>

              <br>
              <% }; %>
          </li>
      </div>
    </ol>

  </div>
    <a href="/urls/new" style="margin-left: 15px;">Add a website to this list &#x2b;</a>

</body>

</html>
